1. Alex Huang Xiaowen Ma
2. it could not build if don't have a initial value and a constructor. If I assigns the default constructor, it will
return 0 and 0;
3. variables in struct is publically accessiable, while class not necessarily so. variables declared public can be 
accessed, but not those decleared private.
4. output:
0
4
0
5. Output:
a.width = 0 and a.length = 0
a.width =  3 and a.length = 7
a.width =  3 and a.length = 7
default: set all values to a default value
second one: set all values to values entered
copy: set the new one's value to the last
6. It does exactly the same thing. The same values are copied. 
